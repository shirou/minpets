// getCode returns codeblock from markdown content.
// code is
//   1. first codebloc(srounded by ```)
//   2. if codeblock does not exists, return all content
export const getCode = (md: string): string => {
  return md;
};
